---
目的: RM-095 PPTX edit を非同期基盤で実行できるようにする
関連ブランチ: feat/rm095-stage5-edit
関連Issue: 未作成
roadmap_item: RM-095 Stage5 PPTX 編集反映
---

- [x] ブランチ作成・初期コミット・push
  - メモ: 既存ブランチ feat/rm095-stage5-edit を流用。初期コミット済み。
- [ ] 計画策定（スコープ・前提の整理）
  - メモ: Plan 承認待ち。非同期基盤（キュー/ワーカー）への組み込み範囲とデータフローを整理する。
- [ ] 設計・実装方針の確定
  - メモ: Plan 承認内容を転記し、同期CLIから非同期ジョブ投入への切り替え方（API/CLIインタフェース、ジョブID管理）を明記する。
  - [ ] 設計・実装方針メモの共有（必要な場合に docs/notes 等へのリンクを記載）
  - [ ] 方針メモを更新するまで以降の stage へ進まないこと
- [ ] 実装
  - メモ: enqueue/worker 実装、ジョブ状態取得、結果書き込みの設計。同期CLIの互換扱い（ブロッキングで結果待ちするか非同期専用にするか）を決める。
- [ ] テスト・検証
  - メモ: 非同期実行フローの回帰確認（キュー投入→処理→出力生成）。可能ならユニット＋統合テスト。
- [ ] ドキュメント更新
  - メモ: CLI/README に非同期利用方法を追記。変更不要の場合も理由を記載してチェックする。
  - [ ] docs/roadmap 配下
  - [ ] docs/requirements 配下（実装結果との整合再確認）
  - [ ] docs/design 配下（実装結果との整合再確認）
  - [ ] docs/runbook 配下
  - [ ] README.md / AGENTS.md
- [ ] 関連Issue 行の更新
  - メモ: Issue 未作成のため対応後に更新。
- [ ] チェックリスト整合確認
  - メモ: 子タスク完了後に整合チェック。
- [ ] PR 作成
  - メモ: PR 番号と URL を記録。ワークフローが未動作の場合のみ理由を記載。

## メモ
- 前提/制約: 既存 edit は同期/直列。非同期化で LLM 呼び出しと適用処理をジョブ化する必要あり。
- 決定と理由: 未定（Plan 承認後に更新）
- リスク(UNCONFIRMED): ジョブ管理の失敗時リトライ、重複適用、出力競合、ログ/監査の欠落。既存同期CLIとの互換性。
- Now/Next: Now=Plan 作成・承認待ち。Next=設計方針確定→実装着手。
- テスト実績/抜け: なし（これから実施）
