---
目的: main の最新変更を取り込み、RM-047 対応の前提を同期する
関連ブランチ: feat/rm047-draft-structuring
関連Issue: #264
roadmap_item: RM-047 テンプレ統合構成生成AI連携
---

- [x] ブランチ作成と初期コミット
  - メモ: feat/rm047-draft-structuring を main から作成済み（継続利用）
- [x] 計画策定（スコープ・前提の整理）
  - メモ: スコープ=main 最新差分の取り込みと最小整合性確認。対象=feat/rm047-draft-structuring ブランチ（コード・docs）。主な手順=① `git fetch` & 差分確認 ② `git merge origin/main` で競合解消（docs は main 版を採用）③ `uv run --extra dev pytest` で回帰確認 ④ ToDo 反映。リスク=競合の取りこぼし／仕様差分見落とし。テスト=pytest。ロールバック=マージコミットを `git reset --hard HEAD^`。承認メッセージ: ユーザー発言「ok」（本スレッド）。
- [x] 設計・実装方針の確定
  - メモ: main の `generate_ready` 基盤更新に追従し、ブランチ固有実装は未着手のため追加調整不要と判断。
- [x] ドキュメント更新（要件・設計）
  - メモ: 競合ファイル（README, docs/design/**, docs/requirements/** など）は main 側の内容を採用。追加変更は無し。
  - [x] docs/requirements 配下
    - メモ: `stage-03-mapping.md` を main 版へ差し替え（`draft_mapping_log.json` へのリネーム反映）。
  - [x] docs/design 配下
    - メモ: CLI リファレンスや Stage3 設計書を main 版へ更新。
- [x] 実装
  - メモ: `git merge origin/main` を実施し、docs 系競合は main 内容を採用する形で解消。
- [x] テスト・検証
  - メモ: `uv run --extra dev pytest`（145 件）成功。
- [x] ドキュメント更新
  - メモ: main との差分確認のみで追加修正不要。後続タスクへの影響なし。
  - [x] docs/roadmap 配下
    - メモ: main 版（テンプレ統合更新）を採用。
  - [x] docs/requirements 配下（実装結果との整合再確認）
    - メモ: Stage3 要件が `draft_mapping_log.json` 系へ更新済み。
  - [x] docs/design 配下（実装結果との整合再確認）
    - メモ: CLI / Stage3 の説明が最新状態であることを確認。
  - [x] docs/runbook 配下
    - メモ: `story-outline-ops.md` を main 版へ更新。
  - [x] README.md / AGENTS.md
    - メモ: README の工程3説明を main 版へ刷新。
- [x] 関連Issue 行の更新
  - メモ: #264 維持で問題なしと判断。
- [ ] PR 作成
  - メモ: PR 作成時に番号と URL を記録する

## メモ
- 計画のみで完了とする場合は、判断者・判断日と次のアクション条件をここに記載する
