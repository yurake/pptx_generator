---
目的: RM-089 stage1-4 Flask Web/API 化 / CORS 設定追加
関連ブランチ: feat/rm089-api-cors
関連Issue: #563
roadmap_item: RM-089 stage1-4 Flask Web/API 化
---

- [x] ブランチ作成・初期コミット・push
  - メモ: ブランチ名や初期コミット内容、push したコミットの内容、差分がない場合はその理由を記入する
    - 必ずmainからブランチを切る
    - 2026-01-27: feat/rm089-api-cors を作成。初期コミット=a2e13e7（ToDo追加）。push済み。
- [x] 計画策定（スコープ・前提の整理）
  - メモ: 承認済み Plan をそのまま転記する。以下の項目を含めること。
    - 対象整理（スコープ、対象ファイル、前提）: Flask API に CORS を追加（localhost:4200 を許可）。対象は src/pptx_generator/api/flask_app.py、pyproject.toml、tests/api/test_flask_app.py。
    - ドキュメント／コード修正方針: flask-cors を導入し create_app で CORS を設定。CORS 対象は localhost:4200 に限定。
    - 確認・共有方法（レビュー、ToDo 更新など）: ToDo に進捗/結果を記録し PR 本文へテスト/UAT結果を記載。
    - 想定影響ファイル: flask_app.py、pyproject.toml、test_flask_app.py（CORS ヘッダ確認）。
    - リスク: CORS 設定が広すぎるとセキュリティ懸念。既存ヘッダの競合リスク。
    - テスト方針: `uv run --extra dev pytest tests/api/test_flask_app.py -k \"cors\"`、`uv tool run diff-cover coverage.xml --compare-branch upstream/main`。
    - ロールバック方法: flask-cors 設定と依存追加を revert。
    - 承認メッセージ ID／リンク: 2026-01-27 ユーザー承認「OK」
- [x] 設計・実装方針の確定
  - メモ: create_app で CORS を設定し、localhost:4200 のみ許可。allow_headers/methods は最小限を維持。
  - [x] 設計・実装方針メモの共有（不要）
  - [x] 方針メモを更新するまで以降の stage へ進まないこと
- [x] 実装
  - メモ: create_app に CORS を追加し localhost:4200 のみ許可。依存に flask-cors を追加。
- [x] テスト・検証
  - メモ: 以下を簡潔に記載する
    - 自動テスト:
      - `uv run --extra dev pytest tests/api/test_flask_app.py -k "cors"`
        - 結果: 1 passed
      - `uv tool run diff-cover coverage.xml --compare-branch upstream/main`
        - 結果: Diff coverage 100%
    - ユーザー経路の手動確認（必要な場合）:
      - `uv run flask --app pptx_generator.api.flask_app run --host 127.0.0.1 --port 8000`
      - `OPTIONS /health, /templates, /prepare, /compose, /gen, /edit` を Origin=http://localhost:4200 で送信
      - 結果: 全エンドポイントで Access-Control-Allow-Origin: http://localhost:4200 を確認（/templates 以降は 401 だがヘッダは付与）
    - 生成物の確認があれば、その方法と結果: レスポンスヘッダで CORS を確認
- [x] ドキュメント更新
  - メモ: 結果と影響範囲を整理し、迷う点は必ずユーザーへ相談した結果を残す
  - メモ: 変更不要の場合も必ず理由をメモに記録して `[x]` を付ける
  - [x] docs/roadmap 配下（変更不要: 既存 RM-089 内の運用更新なし）
  - [x] docs/requirements 配下（変更不要: 要件変更なし）
  - [x] docs/design 配下（変更不要: 設計変更なし）
  - [x] docs/runbook 配下（変更不要: 運用手順変更なし）
  - [x] README.md / AGENTS.md（変更不要: 手順追加なし）
- [x] 関連Issue 行の更新
  - メモ: フロントマターの `関連Issue` を #563 に更新。
- [x] チェックリスト整合確認
  - メモ: Issue 作成と関連更新まで完了済み。残りは PR 作成。
- [ ] PR 作成
  - メモ: PR 番号と URL を記録。ワークフローが未動作の場合のみ理由を記載する。todo-auto-complete が自動更新するため手動でチェックしない。

## メモ
- 連続性メモ（短文化し、更新があれば上書きする）※設計確定・実装完了・テスト完了・PR作成前後など状態変化のたびに更新
  - 前提/制約: CORS は localhost:4200 のみ許可。
  - 決定と理由: flask-cors を導入し create_app に設定。ブラウザ直叩きを許可。
  - リスク(UNCONFIRMED): CORS の許可範囲が不足/過剰になる可能性。
  - Now/Next: 実装・テスト・UAT完了。次は Issue/PR 作成。
  - テスト実績/抜け: pytest 1件パス、diff-cover 100%。
- 計画のみで完了とする場合は、判断者・判断日と次のアクション条件をここに記載する。
