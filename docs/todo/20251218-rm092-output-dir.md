---
目的: RM-092 出力ディレクトリ統一
関連ブランチ: feat/rm092-output-dir
関連Issue: 未作成（作成次第更新）
roadmap_item: RM-092 出力ディレクトリ統一
---

- [x] ブランチ作成・初期コミット・push
  - メモ: ブランチ作成: feat/rm092-output-dir（main 起点）。初期コミット・push 未実施。
    - 必ずmainからブランチを切る
- [ ] 計画策定（スコープ・前提の整理）
  - メモ: 承認済み Plan をそのまま転記する。以下の項目を含めること。
    - 対象整理（スコープ、対象ファイル、前提）: `PPTX_OUTPUT_ROOT` を導入し、API/Web で `PPTX_OUTPUT_ROOT/<transaction_id>/<stage>/<job_id>/` を解決できるようにする。CLI は tx/job なしの従来パス `.pptx/<stage>` を維持し、階層化は今回対応しない。
    - ドキュメント／コード修正方針: PipelineContext の workdir 解決にルート設定を追加し、API/Web で tx/job を含む出力パスを生成可能にする。CLI は現行パスを明示しつつ変更なし。必要に応じて README/CLI リファレンスに設定方法を追記する。
    - 確認・共有方法（レビュー、ToDo 更新など）: 作業後に ToDo 更新。PR で diff レビュー。
    - 想定影響ファイル: `src/pptx_generator/pipeline/base.py`（workdir 解決、env 参照）、`src/pptx_generator/cli_handlers/`（CLI は従来パス明示のみ）、必要に応じ `src/pptx_generator/config`（env 読み）、ドキュメント（README.md または docs/design/cli/cli-command-reference.md）。
    - リスク: ルート変更で API/Web 側のパス依存に影響する可能性。ディレクトリ作成権限不足。env 未設定時の挙動漏れ。
    - テスト方針: workdir 解決の単体テスト（env 無し→`.pptx/<stage>`、env 有り＋tx/job→`<root>/<tx>/<stage>/<job>`）。CLI の従来パス維持確認。可能なら軽い統合テストでパス検証。
    - ロールバック方法: 出力解決変更を 1 コミットにまとめ、問題時は revert。
    - 承認メッセージ ID／リンク: （本スレッドの承認）
- [ ] 設計・実装方針の確定
  - メモ: Plan 承認内容を踏まえた設計・実装方針をここに記載し、ユーザー確認が必要な論点があれば列挙する。
  - [ ] 設計・実装方針メモの共有（必要な場合に docs/notes 等へのリンクを記載）
  - [ ] 方針メモを更新するまで以降の stage へ進まないこと
- [ ] 実装
  - メモ: 実装範囲や未対応事項があれば記載する
- [ ] テスト・検証
  - メモ: 実施したテスト内容と結果を記入する
- [ ] ドキュメント更新
  - メモ: 結果と影響範囲を整理し、迷う点は必ずユーザーへ相談した結果を残す
  - メモ: 変更不要の場合も必ず理由をメモに記録して `[x]` を付ける
  - [ ] docs/roadmap 配下
  - [ ] docs/requirements 配下（実装結果との整合再確認）
  - [ ] docs/design 配下（実装結果との整合再確認）
  - [ ] docs/runbook 配下
  - [ ] README.md / AGENTS.md
- [ ] 関連Issue 行の更新
  - メモ: フロントマターの `関連Issue` が `未作成` の場合は、対応する Issue 番号（例: `#123`）へ更新する。進捗をissueに書き込むものではない。
- [ ] チェックリスト整合確認
  - メモ: 子タスクをすべて完了した親タスクが未チェックになっていないか確認し、必要に応じて `[x]` へ更新する。親タスクのメモに完了内容を残す。
- [ ] PR 作成
  - メモ: PR 番号と URL を記録。ワークフローが未動作の場合のみ理由を記載する。todo-auto-complete が自動更新するため手動でチェックしない。

## メモ
-
