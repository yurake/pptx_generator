---
目的: RM-067 スケジュールスライド自動生成の要件整理と実装準備
関連ブランチ: feat/rm067-schedule-slide
関連Issue: #367
roadmap_item: RM-067 スケジュールスライド自動生成
---

- [x] ブランチ作成・初期コミット・push
  - メモ: main から `feat/rm067-schedule-slide` を作成。初期コミットは未実施、push は Plan 承認後に実施予定。
- [x] 計画策定（スコープ・前提の整理）
  - メモ: 承認済み Plan を転記。
    - 対象整理（スコープ、対象ファイル、前提）: main を feat/rm067-schedule-slide に merge しコンフリクトを解消する。対象は衝突する変更ファイルとその依存。
    - ドキュメント／コード修正方針: merge commit で追従し、両ブランチの意図を尊重して手動解消。
    - 確認・共有方法（レビュー、ToDo 更新など）: ToDo 更新。必要に応じて PR で経緯共有。
    - 想定影響ファイル: コンフリクト発生箇所（現変更ファイル・main 側更新ファイル）。
    - リスク: 変更意図の取り違え、テスト時間不足、サンプル出力差分の取り込み漏れ。
    - テスト方針: 基本は `uv run --extra dev pytest` を想定。時間制約があれば優先テストを相談。
    - ロールバック方法: マージ中は `git merge --abort`、マージ完了後は `git revert -m 1 <merge_commit>` を想定。
    - 承認メッセージ ID／リンク: ユーザー承認（このスレッド）
- [x] 設計・実装方針の確定
  - メモ: スケジュール生成向けに parser/renderer/pipeline を新規追加し、CLI 各コマンドと hook を拡張する方針で進行する。
  - [x] 設計・実装方針メモの共有（必要な場合に docs/notes 等へのリンクを記載）
    - メモ: 本 ToDo に方針を記載済み（スケジュール用パッケージ追加と CLI hook 拡張、サンプルと使用ガイド整備）。
  - [x] 方針メモを更新するまで以降の stage へ進まないこと
    - メモ: 方針確定済みのため後続作業を進行可能。
- [x] 実装
  - メモ: スケジュール向けパーサー/レンダラー/パイプラインを追加し、compose/gen/mapping/prepare/template の CLI と hook manager を拡張。サンプルデータと CLI_USAGE ガイドを追加し、コンテンツインポートの不要テストを整理。
- [x] テスト・検証
  - メモ: `uv run --extra dev pytest`（383 passed, 1 skipped）
- [x] ドキュメント更新
  - メモ: スケジュール機能の利用手順は `src/pptx_generator_schedule/CLI_USAGE.md` に追記済み。他カテゴリは変更不要のため理由を併記して完了扱い。
  - [x] docs/roadmap 配下
    - メモ: RM-067 の範囲内で追加実装を行っておりロードマップ追記なし。
  - [x] docs/requirements 配下（実装結果との整合再確認）
    - メモ: 新規スケジュール機能は既存要件と整合しており追記不要。
  - [x] docs/design 配下（実装結果との整合再確認）
    - メモ: 新規パイプライン構成はコード内のガイドで足りるため設計ドキュメント追記不要。
  - [x] docs/runbook 配下
    - メモ: 運用手順への影響なし。
  - [x] README.md / AGENTS.md
    - メモ: 追加ガイドは専用 CLI_USAGE.md に集約済みで総合 README/AGENTS の更新不要。
- [x] 関連Issue 行の更新
  - メモ: フロントマターの `関連Issue` が `未作成` の場合は、対応する Issue 番号（例: `#123`）へ更新する。進捗をissueに書き込むものではない。
- [x] チェックリスト整合確認
  - メモ: 未完は PR 作成のみであることを確認。
- [ ] PR 作成
  - メモ: PR 番号と URL を記録。ワークフローが未動作の場合のみ理由を記載する。todo-auto-complete が自動更新するため手動でチェックしない。

## メモ
- 計画のみで終了する場合は、判断者・判断日と次のアクション条件をここに記載する。
