---
目的: RM-089 API の成果物パス自動解決（transaction_id 起点、パス指定廃止）
関連ブランチ: feat/rm089-flask-web-api
関連Issue: 未作成
roadmap_item: RM-089 stage1-4 Flask Web/API 化
---

- [ ] ブランチ作成・初期コミット・push
  - メモ: feat/rm089-flask-web-api で継続対応（新規ブランチ不要）
- [ ] 計画策定（スコープ・前提の整理）
  - メモ: 下記設計案を Plan として提示し、承認を得る。
    - 対象整理: Flask API (templates/prepare/compose/gen) のリクエストから内部生成物パス指定を廃止し、transaction_id から自動解決する。
    - 修正方針: 成果物レジストリ（JSON）を `PPTX_OUTPUT_ROOT/<tx>/registry.json` に導入し、各 stage の最新 job と標準成果物パスを記録。ステージ完了時に更新し、次ステージで参照する。
    - 確認方法: API 単体テストでパス未指定＋transaction_idのみのフロー、404/422 エラーを検証。OpenAPI/ドキュメント更新。
    - 想定影響: API I/F 破壊的変更、CLI/クライアントの追従が必要。
    - リスク: レジストリ破損、tx内多重ジョブの扱い、PPTX_OUTPUT_ROOT 未設定での衝突。
    - テスト方針: prepare/compose/gen の正/異常（tx不明・前段なし）、成果物ダウンロードの整合を追加。
    - ロールバック: レジストリ導入コミットを分離し、問題時は revert 可能に。
- [ ] 設計・実装方針の確定
  - メモ: 成果物レジストリ案を確定し、HTTP エラーコードポリシーを 404(tx不明) / 422(前段成果物なし) とする。必要なら docs/notes を用意。
  - [ ] 設計・実装方針メモの共有（必要な場合に docs/notes 等へのリンクを記載）
  - [ ] 方針メモを更新するまで以降の stage へ進まないこと
- [ ] 実装
  - メモ: リクエストスキーマ変更（パス項目削除）、レジストリ導入、tx→前段成果物ルックアップ、エラーコード追加、OpenAPI整合。
- [ ] テスト・検証
  - メモ: API テストに transaction_id のみ指定ケースと 404/422 異常系を追加。
- [ ] ドキュメント更新
  - メモ: 結果と影響範囲を整理し、迷う点は必ずユーザーへ相談した結果を残す
  - メモ: 変更不要の場合も必ず理由をメモに記録して `[x]` を付ける
  - [ ] docs/roadmap 配下
  - [ ] docs/requirements 配下（実装結果との整合再確認）
  - [ ] docs/design 配下（実装結果との整合再確認）
  - [ ] docs/runbook 配下
  - [ ] README.md / AGENTS.md
- [ ] 関連Issue 行の更新
  - メモ: フロントマターの `関連Issue` が `未作成` の場合は、対応する Issue 番号（例: `#123`）へ更新する。進捗をissueに書き込むものではない。
- [ ] チェックリスト整合確認
  - メモ: 子タスクをすべて完了した親タスクが未チェックになっていないか確認し、必要に応じて `[x]` へ更新する。親タスクのメモに完了内容を残す。
- [ ] PR 作成
  - メモ: PR 番号と URL を記録。ワークフローが未動作の場合のみ理由を記載する。todo-auto-complete が自動更新するため手動でチェックしない。

## メモ
- 計画のみで完了とする場合は、判断者・判断日と次のアクション条件をここに記載する。
