# 2025-11-17 CODEOWNERS 設定メモ

## 背景
- PR / Issue での承認者を明示するため、全ファイルに対して @yurake の承認を必須化する必要があった。

## 対応内容
- `.github/CODEOWNERS` を新規作成し、パターン `*` に対して `@yurake` を指定。
- リポジトリ全体で単一オーナーを定義したため、GitHub 上で作成される全 PR で @yurake のレビューが自動的に要求される想定。

## 確認・今後
- CODEOWNERS は GitHub 上でのレビュー要求によってのみ検証可能。運用中に想定外の挙動がある場合はこのファイルを更新する。
