{
  "version": "1",
  "default_policy_id": "default",
  "policies": [
    {
      "id": "default",
      "name": "default-template-ai",
      "description": "Template AI policy with canonical usage tag prompt.",
      "prompt_template": "Classify the layout usage tags using the provided metadata. Respond with JSON containing usage_tags and reason.",
      "static_rules": [
        {
          "layout_name_pattern": ".*Title.*",
          "tags": ["title"]
        },
        {
          "layout_name_pattern": ".*Agenda.*",
          "tags": ["agenda"]
        },
        {
          "layout_name_pattern": ".*executive.*summary.*",
          "tags": ["overview", "content"]
        },
        {
          "layout_name_pattern": null,
          "tags": ["content"]
        }
      ]
    }
  ]
}
